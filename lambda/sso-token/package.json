{
  "name": "sso-token-lambda",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "vitest run --project=sso-token",
    "test:watch": "vitest --project=sso-token",
    "build": "esbuild index.js --bundle --platform=node --target=node24 --format=esm --outfile=.build/index.mjs",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check ."
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.35.0"
  }
}
